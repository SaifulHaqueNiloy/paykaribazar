import 'package:flutter/material.dart';

class ResellerApplicationScreen extends StatelessWidget {
  const ResellerApplicationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reseller Application')),
      body: const Center(child: Text('Reseller Application Screen')),
    );
  }
}
